# Target the stable Fedora 41 to avoid Rawhide instability
include("/etc/mock/fedora-41-@ARCH@.cfg")

# This is the critical fix for your current error:
# It forces DNF to use standard metadata instead of the corrupted zchunk files
config_opts['dnf_options'] = ['--setopt=zchunk=False']