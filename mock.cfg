# Target stable Fedora 41 to avoid Rawhide sync issues
include("/etc/mock/fedora-41-@ARCH@.cfg")

# Force DNF to ignore zchunk metadata, bypassing the "Unable to read zchunk lead" error
config_opts['dnf_options'] = ['--setopt=zchunk=False']